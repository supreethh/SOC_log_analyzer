# SOC_log_analyzer
A Python-based log analysis tool that parses structured log files, supports filtering by timestamp, message content, and severity level, and includes basic security detections such as brute-force attack identification along with summarized reporting.
